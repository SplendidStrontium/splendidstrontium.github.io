---
layout: default
title: "Wiki — What Time Cannot Touch"
permalink: /tce/wiki/b0/
---

<div class="wiki-index">

<h1>What Time Cannot Touch</h1>
<p>Book 0 of <em>The Convergent Eigenspace</em>.</p>

<hr>

{% assign b0_entries = site.wiki | where: "book", "b0" %}

{% assign people = b0_entries | where: "category", "person" | sort: "title" %}
{% assign places = b0_entries | where: "category", "place" | sort: "title" %}
{% assign things = b0_entries | where: "category", "thing" | sort: "title" %}

{% if people.size > 0 %}
<h2>People</h2>
<ul class="wiki-entry-list">
  {% for entry in people %}
  <li>
    <a href="{{ entry.url }}">{{ entry.title }}</a>
    {% if entry.description %}<span class="wiki-entry-desc"> — {{ entry.description }}</span>{% endif %}
  </li>
  {% endfor %}
</ul>
{% endif %}

{% if places.size > 0 %}
<h2>Places</h2>
<ul class="wiki-entry-list">
  {% for entry in places %}
  <li>
    <a href="{{ entry.url }}">{{ entry.title }}</a>
    {% if entry.description %}<span class="wiki-entry-desc"> — {{ entry.description }}</span>{% endif %}
  </li>
  {% endfor %}
</ul>
{% endif %}

{% if things.size > 0 %}
<h2>Things</h2>
<ul class="wiki-entry-list">
  {% for entry in things %}
  <li>
    <a href="{{ entry.url }}">{{ entry.title }}</a>
    {% if entry.description %}<span class="wiki-entry-desc"> — {{ entry.description }}</span>{% endif %}
  </li>
  {% endfor %}
</ul>
{% endif %}

<hr>

<p><a href="/tce/wiki/">&larr; Back to Wiki Index</a></p>

</div>
